pre-commit:
  parallel: true
  commands:
    eslint:
      files: git diff --name-only @{push}
      glob: "*.ts"
      run: npx eslint {files}
    prettier:
      files: git diff --name-only @{push}
      glob: "*.ts"
      run: npx prettier --write {files}
